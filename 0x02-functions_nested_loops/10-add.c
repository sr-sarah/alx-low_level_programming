#include "main.h"
/**
 * add - the function adds two integers
 * @a: the first numbers to add
 * @b: the second number to add
 *
 * Return: the sum of the two numbers a & b.
 */
int add(int a, int b)
{
int sum;
sum = a + b;
return (sum);
}
