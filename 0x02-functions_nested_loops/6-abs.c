#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @n : the integer to compute its absolute value
 * Return: 0 Succcess.
 */
int _abs(int n)
{
if (n >= 0)
{
/*_putchar('n');*/
return (n);
}
else
{
/*_putchar('-');*/
_putchar('n');
return (-n);
}
}
