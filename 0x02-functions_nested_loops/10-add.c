#include "main.h"
/**
 *_add(int a, int b) - adds two integers.
 *
 * @a: the first numbers to add
 * @b: the second number to add
 * Return : the result of the operation
 */
int add(int a, int b)
{
s = a + b;
return (s);
}
