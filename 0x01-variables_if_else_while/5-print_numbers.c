#include <stdio.h>
/**
 * main - Entry point of the program
 *program that prints single digit numbers
 * Return: Always 0 (Success)
 */
int main(void)
{
int a;
for (a = 0; a < 10; a++)
{
printf("%d", n);
}
return (0);
}
